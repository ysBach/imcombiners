# kernels.var_1d { #imcombiners.kernels.var_1d }

```python
kernels.var_1d(values, *, ddof=0, return_mean=False, validate=True)
```

Return the NaN-aware variance of a 1-D value vector.

If `return_mean` is `True`, return ``(variance, mean)`` from one Rust
accumulation pass.
