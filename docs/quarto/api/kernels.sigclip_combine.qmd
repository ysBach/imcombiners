# kernels.sigclip_combine { #imcombiners.kernels.sigclip_combine }

```python
kernels.sigclip_combine(
    arr,
    *,
    mask=None,
    combine,
    sigma=(3.0, 3.0),
    maxiters=5,
    ddof=0,
    nkeep=1,
    maxrej=None,
    cenfunc='median',
    clip_cen=None,
    stdfunc='std',
    revert_on_nkeep=True,
    validate=True,
)
```

Return an output-only sigma-clipped mean or median.